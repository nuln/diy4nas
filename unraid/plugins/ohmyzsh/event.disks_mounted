#!/bin/sh

CONFIG_FILE="/boot/config/plugins/ohmyzsh/ohmyzsh.conf"
SERVICE_SCRIPT="/etc/rc.d/rc.ohmyzsh"

if [ ! -f "$CONFIG_FILE" ] || [ ! -x "$SERVICE_SCRIPT" ]; then
    exit 0
fi

. "$CONFIG_FILE"

if [ "${AUTOSTART:-yes}" = "yes" ]; then
    "$SERVICE_SCRIPT" setup >/dev/null 2>&1
fi
